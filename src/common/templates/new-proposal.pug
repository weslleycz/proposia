html
  head
    title Nova Proposta
    style.
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { width: 80%; margin: 20px auto; padding: 20px; border: 1px solid #ddd; border-radius: 8px; background-color: #f9f9f9; }
      h1, h2 { color: #0056b3; }
      .item-table { width: 100%; border-collapse: collapse; margin-top: 20px; }
      .item-table th, .item-table td { border: 1px solid #ddd; padding: 8px; text-align: left; }
      .item-table th { background-color: #e9e9e9; }
      .total { font-weight: bold; margin-top: 20px; text-align: right; }
  body
    .container
      h1 Olá, #{proposal.client.name}!
      p Uma nova proposta foi criada para você.
      h2 Detalhes da Proposta:
      p
        strong Título:
        |  #{proposal.title}
      p
        strong Descrição:
        |  #{proposal.description}

      if proposal.items && proposal.items.length > 0
        h3 Itens da Proposta:
        table.item-table
          thead
            tr
              th Descrição
              th Quantidade
              th Preço Unitário
              th Total
          tbody
            each item in proposal.items
              tr
                td #{item.description}
                td #{item.quantity}
                td #{item.unitPrice.toFixed(2)}
                td #{(item.quantity * item.unitPrice).toFixed(2)}
        .total
          p Valor Total da Proposta: R$ #{proposal.total.toFixed(2)}

      p Para mais detalhes, acesse nosso portal.
      p Atenciosamente,
      p Equipe Proposia